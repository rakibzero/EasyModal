# Example environment variables for the brownfield comfyapp.py (NOT required by the Deploy app).
# The Deploy app stores keys in ~/.wan22-deploy/config.json via the UI.

# HuggingFace token (read) — used by comfyapp.py for model downloads.
HF_TOKEN=hf_your_read_token_here
